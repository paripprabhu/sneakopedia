# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local — it is listed in .gitignore.

# ── MongoDB Atlas ─────────────────────────────────────────────────────────────
# Connection string from Atlas → Connect → Drivers.
# Format: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/sneakopedia?retryWrites=true&w=majority

# ── CORS (anti-scraping) ──────────────────────────────────────────────────────
# Your production domain — only this origin is allowed to call /api/* from a browser.
# Omit trailing slash. Leave blank to allow all origins (not recommended in production).
# Also set this in Vercel → Settings → Environment Variables.
APP_ORIGIN=https://your-domain.vercel.app
